# Temporal Mechanics Configuration Service

- Author: Robinson Collado
- This project was ran with bun
- To run: `bun dev`
- For the project to work, you need to store an api key into local storage with the key `x-api-key`. Ex. `localStorage.setItem('x-api-key', '<YOUR-API-KEY>')`
